please execute this code with the version 1.22 Go forwards

since version 1.22 it is possible to do something like http.HandleFunc("GET /", getDolarPrice)
instead of http.HandleFunc("/", getDolarPrice)
